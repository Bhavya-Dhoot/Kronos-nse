---
phase: 01-scaffold-score
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - variance/__init__.py
  - variance/collectors/__init__.py
  - variance/tests/__init__.py
  - pyproject.toml
  - config/base.yaml
autonomous: true
requirements:
  - SCF-01
  - SCF-02
  - SCF-03
user_setup: []

must_haves:
  truths:
    - "variance/ directory tree exists with all __init__.py files"
    - "MVE dependencies (NseIndiaApi, yfinance, scrapling, playwright, pandas-ta) installable from pyproject.toml"
    - "config/base.yaml has variance: section with all MVE settings"
  artifacts:
    - path: "variance/__init__.py"
      provides: "Package marker for variance module"
      min_lines: 1
    - path: "variance/collectors/__init__.py"
      provides: "Package marker for collectors subpackage"
      min_lines: 1
    - path: "variance/tests/__init__.py"
      provides: "Package marker for tests subpackage"
      min_lines: 1
    - path: "pyproject.toml"
      provides: "MVE dependency declarations"
      contains_pattern: "NseIndiaApi|yfinance|scrapling|playwright|pandas-ta"
    - path: "config/base.yaml"
      provides: "MVE configuration section"
      contains_pattern: "variance:"
  key_links:
    - from: "pyproject.toml"
      to: "config/base.yaml"
      via: "MVE deps and config are separate artifacts but both define the phase foundation"
      pattern: "NseIndiaApi.*yfinance|variance:"

---

<objective>
Create the MVE scaffolding: variance/ directory tree, MVE dependencies in pyproject.toml, and MVE configuration section in config/base.yaml.

Purpose: Establish the physical structure and configuration that all downstream MVE components depend on. This is the first thing created so nothing else can proceed.
Output: variance/ package tree, updated pyproject.toml, updated config/base.yaml
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scaffold-score/01-CONTEXT.md
@config/base.yaml
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create variance/ directory tree with __init__.py files</name>
  <files>
    variance/__init__.py
    variance/collectors/__init__.py
    variance/tests/__init__.py
    variance/collectors/
    variance/tests/
  </files>
  <read_first>
    @.planning/phases/01-scaffold-score/01-CONTEXT.md (decisions, domain boundary)
  </read_first>
  <action>
    Create the following directory structure under the project root:

    ```
    variance/
      __init__.py              # Package docstring: "Market Variance Engine (MVE) — real-time variance scoring."
      collectors/
        __init__.py            # Package docstring: "Dimension-specific collectors for MVE."
      tests/
        __init__.py            # Package docstring: "Tests for MVE components."
    ```

    Each __init__.py must have a module-level docstring and `from __future__ import annotations` import.

    The variance/ directory is a top-level package at the project root (same level as api/, config/, data/).
    Do NOT nest it under src/ — it's a standalone package.
  </action>
  <verify>
    <automated>
      test -f variance/__init__.py && test -f variance/collectors/__init__.py && test -f variance/tests/__init__.py
    </automated>
  </verify>
  <acceptance_criteria>
    - variance/__init__.py exists with `from __future__ import annotations`
    - variance/collectors/__init__.py exists with `from __future__ import annotations`
    - variance/tests/__init__.py exists with `from __future__ import annotations`
    - Each file has a module-level docstring
  </acceptance_criteria>
  <done>
    variance/ directory tree exists with all 3 __init__.py files as specified. (SCF-01 ✓)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add MVE dependencies to pyproject.toml</name>
  <files>
    pyproject.toml
  </files>
  <read_first>
    @pyproject.toml (full file — read existing dependencies to match formatting)
  </read_first>
  <action>
    Append the following dependencies to the `[project]dependencies` list in pyproject.toml.
    Add them after the existing `"lightgbm>=4.3"` line, maintaining alphabetical order within the MVE group.
    Per D-09, use `pandas-ta` instead of `ta-lib` to avoid C header compilation.
    Per D-10, exact strings to add:

    ```
        "nse @ git+https://github.com/BennyThadikaran/NseIndiaApi.git",
        "yfinance>=0.2.40",
        "scrapling>=0.4.0",
        "playwright>=1.40",
        "pandas-ta>=0.3.14b",
    ```

    Also add `playwright` to the `[tool.ruff.lint]` ignore list if not already present
    (scrapling/playwright import patterns may trigger unused-import warnings).
    Do NOT remove or modify any existing dependencies.
  </action>
  <verify>
    <automated>grep -q "NseIndiaApi" pyproject.toml && grep -q "yfinance" pyproject.toml && grep -q "pandas-ta" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `"nse @ git+https://github.com/BennyThadikaran/NseIndiaApi.git"`
    - pyproject.toml contains `"yfinance>=0.2.40"`
    - pyproject.toml contains `"scrapling>=0.4.0"`
    - pyproject.toml contains `"playwright>=1.40"`
    - pyproject.toml contains `"pandas-ta>=0.3.14b"`
    - All 5 entries are within the `[project]dependencies` list
  </acceptance_criteria>
  <done>
    pyproject.toml has all 5 MVE dependencies installed. (SCF-02 ✓, D-08 ✓, D-09 ✓, D-10 ✓)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add MVE config section to config/base.yaml</name>
  <files>
    config/base.yaml
  </files>
  <read_first>
    @config/base.yaml (full file — read existing config patterns)
  </read_first>
  <action>
    Append a `variance:` section at the END of config/base.yaml, after the `scheduler:` section.
    Per D-01: MVE config lives in config/base.yaml extending the existing file.
    Per D-02: Use the same YAML env var interpolation pattern (`${VAR_NAME:default}`).
    Per D-03: Config structure follows the agreed spec:

    ```yaml
    variance:
      enabled: true

      cache_ttl_seconds: 60

      poll_interval_seconds:
        vix: 60
        options: 300
        fii_dii: 1800
        oi: 300
        gift_nifty: 300
        global_markets: 300
        macro: 300

      weights:
        vix: 0.25
        options: 0.20
        institutional: 0.25
        gift_nifty: 0.15
        global_macro: 0.15

      modification:
        temperature_base: 0.015
        vix_baseline: 15
        temperature_cap: 0.3
        band_width_per_vix_point: 0.008
        signal_base_threshold: 0.005
        signal_threshold_per_vix_point: 0.0002
        confidence_panic: "LOW"
        confidence_fear: "LOW"

      gift_nifty:
        primary_url: "https://groww.in/markets/gift-nifty"
        fallback_url: "https://niftytrader.in/gift-nifty"

      mve_history:
        retention_days: 30
    ```

    Use the same indentation style (2 spaces) as the rest of the file.
    Use the same `key: value` spacing convention as existing entries.
    Do NOT modify any existing sections.
  </action>
  <verify>
    <automated>grep -q "^variance:" config/base.yaml</automated>
  </verify>
  <acceptance_criteria>
    - config/base.yaml contains a `variance:` top-level key
    - variance section contains all sub-keys: `enabled`, `cache_ttl_seconds`, `poll_interval_seconds`, `weights`, `modification`, `gift_nifty`, `mve_history`
    - `poll_interval_seconds` has all 7 dimension keys
    - `weights` has all 5 dimension weights summing to 1.0
    - Indentation consistent with existing config (2-space YAML)
  </acceptance_criteria>
  <done>
    config/base.yaml has complete variance: section with all MVE settings. (SCF-03 ✓, D-01 ✓, D-02 ✓, D-03 ✓)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pyproject.toml → supply chain | Adding external Python dependencies introduces supply-chain risk for downstream collectors |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | pyproject.toml deps | accept | Dependencies are from well-known sources (PyPI, GitHub). Noting risk: any of NseIndiaApi, yfinance, scrapling, playwright, or pandas-ta could be compromised upstream. Accept for v1; supply-chain locking (hashes/pip-audit) can be added later. |
| T-01-02 | T | config/base.yaml | accept | Config files are version-controlled and readable only by the system user. No secrets in this config section (poll intervals, weights, URLs only). |
</threat_model>

<verification>
1. `ls -la variance/ variance/collectors/ variance/tests/` — all directories exist, all __init__.py files present
2. `grep -c "NseIndiaApi\|yfinance\|scrapling\|playwright\|pandas-ta" pyproject.toml` — 5 dependencies found
3. `grep -c "variance:" config/base.yaml` — variance section header present
4. `python -c "import yaml; yaml.safe_load(open('config/base.yaml'))"` — YAML parses without errors
</verification>

<success_criteria>
- [ ] All 3 __init__.py files exist with docstrings and future annotations import
- [ ] pyproject.toml has 5 MVE dependencies added
- [ ] config/base.yaml has complete variance: section
- [ ] pip install passes with new dependencies (or at minimum syntax is valid TOML)
- [ ] Python YAML loader can parse the updated config file
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-score/01-01-SUMMARY.md`
</output>
