---
phase: 01-scaffold-score
plan: 04
type: execute
wave: 3
depends_on:
  - 02
  - 03
files_modified:
  - variance/tests/test_base_collector.py
  - variance/tests/test_score.py
autonomous: true
requirements:
  - BASE-03
user_setup: []

must_haves:
  truths:
    - "8 scoring math tests pass (composite weighting, stale handling, market state, 5 derived properties)"
    - "BaseVarianceCollector circuit-breaker tests pass"
    - "Tests use pytest with asyncio support"
  artifacts:
    - path: "variance/tests/test_base_collector.py"
      provides: "Unit tests for BaseVarianceCollector"
      min_lines: 60
    - path: "variance/tests/test_score.py"
      provides: "Unit tests for MarketVarianceScore scoring math"
      min_lines: 120
  key_links:
    - from: "variance/tests/test_base_collector.py"
      to: "variance/base_collector.py"
      via: "Tests import and subclass BaseVarianceCollector"
      pattern: "from variance.base_collector import"
    - from: "variance/tests/test_score.py"
      to: "variance/score.py"
      via: "Tests import MarketVarianceScore"
      pattern: "from variance.score import"
    - from: "variance/tests/test_score.py"
      to: "variance/schemas.py"
      via: "Tests construct DimensionScore fixtures"
      pattern: "from variance.schemas import"

---

<objective>
Write and verify all Phase 1 tests: base collector behavior tests and scoring math tests.

Purpose: Tests are the verification gate for this phase (8 tests passing for score math). Every downstream phase depends on the BaseCollector interface and MarketVarianceScore math being correct. These tests lock in correctness so collectors built in Phases 2-5 can trust their base class.
Output: variance/tests/test_base_collector.py, variance/tests/test_score.py
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scaffold-score/01-CONTEXT.md
@variance/base_collector.py
@variance/score.py
@variance/schemas.py
@tests/unit/test_storage.py (reference: existing test patterns)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write base collector tests</name>
  <files>
    variance/tests/test_base_collector.py
  </files>
  <read_first>
    @variance/base_collector.py (full file — class to test)
    @variance/schemas.py (ParseResult type)
    @tests/unit/test_storage.py (reference: existing project test patterns, AsyncMock usage)
  </read_first>
  <behavior>
    - Test 1: Instantiation fails for ABC (TypeError)
    - Test 2: Concrete subclass can be instantiated
    - Test 3: poll() calls fetch() then parse() then score() in that order
    - Test 4: poll() returns ParseResult with correct fields
    - Test 5: Circuit-breaker trips after 5 consecutive errors (is_available=False)
    - Test 6: Circuit-breaker resets after successful poll
    - Test 7: poll() returns stale result (is_stale=True) when fetch() raises and cached value exists
  </behavior>
  <action>
    Create `variance/tests/test_base_collector.py`:

    **Imports:**
    ```python
    from __future__ import annotations

    import asyncio
    from datetime import datetime, timezone
    from typing import Any
    from unittest.mock import AsyncMock, MagicMock, patch

    import pytest

    from variance.base_collector import BaseVarianceCollector
    from variance.schemas import ParseResult
    ```

    **Test fixtures:**
    - `class MockCollector(BaseVarianceCollector)` — minimal concrete subclass for testing:
      - `fetch()` returns `{"data": 42}`
      - `parse()` returns a ParseResult with raw_value=42, normalized=0.5, direction=1, magnitude=0.5, detail={}, source="mock", as_of=current UTC ISO
      - `score()` returns 0.5

    - `pass through __init__` fixture that creates a MockCollector with name="test", poll_interval=1

    **Test 1: `test_cannot_instantiate_abc()`**
    Verify `TypeError` is raised when trying to instantiate `BaseVarianceCollector` directly.

    **Test 2: `test_concrete_subclass_can_be_instantiated(mock_collector)`**
    Assert mock_collector is an instance of BaseVarianceCollector.

    **Test 3: `test_poll_calls_fetch_parse_score_in_order(mock_collector)`**
    Mock fetch, parse, and score to track call order. Call poll(). Assert all three called with fetch → parse → score sequence.
    Use AsyncMock for fetch, regular MagicMock for parse and score.

    **Test 4: `test_poll_returns_parse_result(mock_collector)`**
    Call poll(). Assert result is a dict with keys: raw_value, normalized, direction, magnitude, detail, source, as_of.

    **Test 5: `test_circuit_breaker_trips_after_5_errors(mock_collector)`**
    - Mock fetch to raise Exception.
    - Call poll() 5 times (each should raise or return stale).
    - Assert `mock_collector.is_available` is False after 5 errors.
    - Call poll() a 6th time — assert it immediately returns or raises, and is_available stays False.

    **Test 6: `test_circuit_breaker_resets_on_success(mock_collector)`**
    - Mock fetch to raise 3 times, then succeed.
    - Call poll() 3 times.
    - After 4th call, fetch succeeds — assert is_available is True.

    **Test 7: `test_stale_result_on_error_with_cache(mock_collector)`**
    - Call poll() once successfully (stores _last_successful_result).
    - Mock fetch to raise.
    - Call poll() again — assert returned ParseResult has `source` field and a non-None value.
    - Assert `_consecutive_errors` is 1.

    Follow existing project test convention: use `@pytest.mark.asyncio` on all async tests, `AsyncMock` for async mocks, `MagicMock` for sync mocks.

    The test file should be runnable with: `python -m pytest variance/tests/test_base_collector.py -v`
  </action>
  <verify>
    <automated>python -m pytest variance/tests/test_base_collector.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - test_cannot_instantiate_abc passes
    - test_concrete_subclass_can_be_instantiated passes
    - test_poll_calls_fetch_parse_score_in_order passes
    - test_poll_returns_parse_result passes
    - test_circuit_breaker_trips_after_5_errors passes
    - test_circuit_breaker_resets_on_success passes
    - test_stale_result_on_error_with_cache passes
    - All 7 tests pass with `pytest -v`
  </acceptance_criteria>
  <done>
    All base collector tests pass. (BASE-01 ✓ via tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write scoring math tests (8 test cases)</name>
  <files>
    variance/tests/test_score.py
  </files>
  <read_first>
    @variance/score.py (full file — MarketVarianceScore implementation)
    @variance/schemas.py (DimensionScore type)
    @.planning/phases/01-scaffold-score/01-CONTEXT.md (D-15: derived properties, D-16: market state, D-17: stale half-weight, D-19: 8 tests)
  </read_first>
  <behavior>
    - Test 1: build() returns correct weighted composite with equal weights
    - Test 2: build() applies half-weight to stale dimensions and normalizes denominator
    - Test 3: Market state classified as PANIC (VIX=30, composite=-0.5)
    - Test 4: Market state classified as FEAR (VIX=23, composite=-0.5)
    - Test 5: Market state classified as BULL_RUN (VIX=12, composite=0.5)
    - Test 6: temperature_adjustment computes correctly at VIX=20, VIX=30, VIX=10
    - Test 7: band_width_multiplier and signal_threshold derived from VIX
    - Test 8: confidence_override returns "LOW" for PANIC/FEAR, None for BULL_RUN
  </behavior>
  <action>
    Create `variance/tests/test_score.py`:

    **Imports:**
    ```python
    from __future__ import annotations

    from datetime import datetime, timezone
    from typing import Any

    import pytest

    from variance.score import MarketVarianceScore, MarketState
    from variance.schemas import DimensionScore
    ```

    **Helper fixture:**
    ```python
    def make_dim(
        name: str,
        score: float,
        weight: float = 0.2,
        is_stale: bool = False,
    ) -> DimensionScore:
        return DimensionScore(
            name=name, score=score, weight=weight,
            is_stale=is_stale, detail={},
            collected_at=datetime.now(timezone.utc).isoformat(),
        )
    ```

    **Test 1: `test_weighted_composite_all_equal()`**
    - 5 dimensions: vix=1.0, options=0.5, institutional=-0.5, gift_nifty=0.0, global_macro=-1.0
    - All weight=0.2
    - Composite = (1.0 + 0.5 - 0.5 + 0.0 - 1.0) * 0.2 = 0.0
    - Assert composite ≈ 0.0

    **Test 2: `test_stale_dimension_half_weight()`** (D-17, BASE-03)
    - 2 dimensions: vix=1.0 (weight=0.6, not stale), options=-1.0 (weight=0.4, stale)
    - Effective weights: vix=0.6, options=0.2 (halved)
    - Denominator: 0.6 + 0.2 = 0.8
    - Composite = (0.6*1.0 + 0.2*(-1.0)) / 0.8 = (0.6 - 0.2) / 0.8 = 0.5
    - Assert composite ≈ 0.5

    **Test 3: `test_market_state_panic()`** (D-16)
    - VIX=30, composite=-0.5
    - Assert market_state == MarketState.PANIC

    **Test 4: `test_market_state_fear()`** (D-16)
    - VIX=23, composite=-0.5
    - Assert market_state == MarketState.FEAR

    **Test 5: `test_market_state_bull_run()`** (D-16)
    - VIX=12, composite=0.5
    - Assert market_state == MarketState.BULL_RUN

    Also test NEUTRAL:
    - VIX=15, composite=0.0 → NEUTRAL

    **Test 6: `test_temperature_adjustment()`** (D-15)
    - VIX=10 (below 15): adjustment = 0.0
    - VIX=20 (5 above 15): adjustment = 5 * 0.015 = 0.075
    - VIX=35 (20 above 15): adjustment = min(20 * 0.015, 0.3) = 0.3
    - VIX=None: adjustment = 0.0

    **Test 7: `test_band_and_signal_derived_properties()`** (D-15)
    - VIX=10 (≤15): band_multiplier=1.0, signal_threshold=0.005
    - VIX=25 (10 above 15): band_multiplier=1.0+10*0.008=1.08, signal_threshold=0.005+10*0.0002=0.007
    - VIX=None: band_multiplier=1.0, signal_threshold=0.005

    **Test 8: `test_confidence_override()`** (D-15)
    - PANIC state: confidence_override == "LOW"
    - FEAR state: confidence_override == "LOW"
    - BULL_RUN state: confidence_override is None
    - UNCERTAIN with |composite| < 0.5: confidence_override == "LOW"
    - UNCERTAIN with |composite| >= 0.5: confidence_override is None

    Each test builds an MVS via `MarketVarianceScore.build(dimensions=[...], vix_value=...)` then asserts on the properties.

    Use `pytest.approx` for float comparisons (rel=1e-5).
    Tests should be runnable with: `python -m pytest variance/tests/test_score.py -v`
  </action>
  <verify>
    <automated>python -m pytest variance/tests/test_score.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - test_weighted_composite_all_equal passes
    - test_stale_dimension_half_weight passes (stale gets half weight, denominator normalized)
    - test_market_state_panic passes (VIX > 28)
    - test_market_state_fear passes (VIX > 22 & composite < -0.4)
    - test_market_state_bull_run passes (VIX < 14 & composite > 0.4)
    - test_temperature_adjustment passes (0.015/point above 15, capped at 0.3)
    - test_band_and_signal_derived_properties passes
    - test_confidence_override passes (PANIC/FEAR → LOW)
    - All 8 tests pass with `pytest -v`
  </acceptance_criteria>
  <done>
    All 8 scoring math tests pass — composite weighting, stale half-weight handling, market state classification, and all 5 derived properties verified. (BASE-03 ✓, D-15 ✓, D-16 ✓, D-17 ✓, D-19 ✓, D-20 ✓)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test code → production code | Tests import and exercise production modules |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | — | test_score.py | accept | Tests only verify correct math. No security boundary crossed. All external deps mocked. |
| T-01-09 | — | test_base_collector.py | accept | Tests only verify ABC interface contract. No external calls made. All mocks. |
</threat_model>

<verification>
1. `python -m pytest variance/tests/test_base_collector.py -v` — all 7 collector tests pass
2. `python -m pytest variance/tests/test_score.py -v` — all 8 scoring math tests pass
3. `python -m pytest variance/tests/ -v` — all 15 tests pass
</verification>

<success_criteria>
- [ ] All 7 base collector tests pass (ABC, instantiation, poll flow, circuit-breaker, stale values)
- [ ] All 8 scoring math tests pass (composite, stale half-weight, market states, 5 derived properties)
- [ ] Total 15 tests passing for this phase
- [ ] Tests use pytest with asyncio support
- [ ] All external dependencies mocked (no live Redis/DB required)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-score/01-04-SUMMARY.md`
</output>
