---
phase: 09-dqg-system-test
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backtest_mve.py
autonomous: true
requirements:
  - DQG-04
user_setup: []

must_haves:
  truths:
    - "Backtest script runs a single prediction pass, then compares modified vs unmodified results"
    - "Reports MAE of pred_close (primary), directional accuracy, average confidence for each state"
    - "Three states compared: Unmodified, Modified with MVE, Difference (modified - unmodified)"
    - "Symbols and timeframes read from existing backtest config"
    - "Output: console summary table + JSON file in backtest output directory"
    - "Positive difference means MVE improved prediction accuracy"
  artifacts:
    - path: "scripts/backtest_mve.py"
      provides: "MVE backtesting script with modified vs unmodified comparison"
      contains: "def main"
    - path: "backtest/runner.py"
      provides: "Existing BacktestRunner.run() — reused for prediction pass"
      contains: "class BacktestRunner"
  key_links:
    - from: "scripts/backtest_mve.py"
      to: "variance/modifier.py :: PredictionModifier.modify_post_inference()"
      via: "Applied to each prediction result for MVE-modified comparison"
      pattern: "modify_post_inference"
    - from: "scripts/backtest_mve.py"
      to: "backtest/runner.py :: BacktestRunner.run()"
      via: "Reuses inference context and prediction pipeline"
      pattern: "BacktestRunner"
---

<objective>
Build a backtesting script that compares Kronos prediction accuracy with and without MVE modifications — producing a clear metric comparison (MAE, directional accuracy, confidence) across three states: unmodified, modified with MVE, and the difference.

**Purpose:** Quantify the impact of MVE modifications on prediction quality. Answer the question: "Does MVE actually improve predictions?" by running the same predictions through PredictionModifier.modify_post_inference() and measuring the difference.

**Output:**
- `scripts/backtest_mve.py` — Standalone backtest script
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/mnt/f/wsl/work/kronos-nse/.planning/phases/09-dqg-system-test/09-CONTEXT.md
@/mnt/f/wsl/work/kronos-nse/.planning/STATE.md
@/mnt/f/wsl/work/kronos-nse/.planning/ROADMAP.md
@/mnt/f/wsl/work/kronos-nse/backtest/runner.py
@/mnt/f/wsl/work/kronos-nse/variance/modifier.py
@/mnt/f/wsl/work/kronos-nse/scripts/
@/mnt/f/wsl/work/kronos-nse/tests/

<interfaces>
From `backtest/runner.py` — BacktestRunner pattern:

```python
class BacktestRunner:
    def __init__(self, config: dict[str, Any], inference_ctx: Any) -> None:
        self.config = config
        self.ctx = inference_ctx
    
    async def run(self) -> dict[str, Any]:
        # Iterates configured symbols, calls self.ctx.engine.predict()
        # Returns {"symbols_tested": N, "mean_mae": float, "mean_directional_acc": float, ...}
```

Existing backtest config keys (from config/base.yaml, accessed via backtest section):
```yaml
backtest:
  universe: NIFTY50  # or from collector.universe
  timeframe: 5min
  start_date: 2024-01-01
  end_date: 2024-06-01
  max_symbols: 5
```

From `variance/modifier.py` — PredictionModifier.modify_post_inference():
```python
class PredictionModifier:
    def __init__(self, mve: MarketVarianceEngine | None = None) -> None:
        self._mve = mve
    
    def modify_post_inference(self, prediction: dict) -> dict:
        # Applies bias → bands → constraints → confidence to pred_close, pred_high, pred_low
        # Returns modified prediction dict
        # If MVE not ready or last_mvs is None: returns prediction unchanged (no-op)
```

From `scripts/` directory — existing script patterns:
```bash
ls scripts/
# checkpoints/  seed_instruments.py  start.sh  ...
```

From `model/engine.py` — KronosEngine.predict() result dict shape (relevant fields):
```python
result = {
    "symbol": str,
    "pred_open": list[float],
    "pred_high": list[float],
    "pred_low": list[float],
    "pred_close": list[float],
    "pred_volume": list[float],
    "pred_timestamps": list[str],
    "temperature": float,
    "confidence": str,
    # ... other metadata
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MVE backtesting script</name>
  <files>scripts/backtest_mve.py</files>
  <action>
Create `scripts/backtest_mve.py` — a standalone CLI script that runs a single backtest pass and compares modified vs unmodified predictions.

**Script structure:**

```python
#!/usr/bin/env python3
"""MVE Backtest: compare prediction accuracy with/without MVE modifications.

Per D-18: Single backtest run — get predictions once, then compare modified
vs unmodified by applying modify_post_inference() on the result.

Usage:
    python scripts/backtest_mve.py [--config config/base.yaml]
"""

from __future__ import annotations

import argparse
import asyncio
import json
import logging
import os
import sys
from datetime import datetime
from pathlib import Path
from typing import Any

import numpy as np
import yaml

# Ensure project root is on sys.path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from backtest.runner import BacktestRunner
from model.factory import build_inference_context, close_inference_context
from variance.modifier import PredictionModifier

logger = logging.getLogger(__name__)


def compute_mae(actual: list[float], predicted: list[float]) -> float | None:
    """Compute Mean Absolute Error between actual and predicted values."""
    if not actual or not predicted:
        return None
    return float(np.mean(np.abs(np.array(predicted) - np.array(actual))))


def compute_directional_accuracy(
    actual: list[float], predicted: list[float]
) -> float | None:
    """Compute directional accuracy (fraction of bars where direction matches)."""
    if len(actual) < 2 or len(predicted) < 2:
        return None
    actual_dirs = [1 if actual[i] > actual[i - 1] else 0 for i in range(1, len(actual))]
    pred_dirs = [1 if predicted[i] > predicted[i - 1] else 0 for i in range(1, len(predicted))]
    matches = sum(1 for a, p in zip(actual_dirs, pred_dirs) if a == p)
    return matches / len(actual_dirs) if actual_dirs else None


async def run_backtest(
    config_path: str,
    output_dir: str,
    max_symbols: int = 5,
) -> dict[str, Any]:
    """Run MVE backtest: predict once, compare modified vs unmodified.
    
    Per D-18: Single backtest run — get predictions once, then apply
    modify_post_inference() on the result for comparison.
    
    Parameters
    ----------
    config_path : str
        Path to the base YAML config file.
    output_dir : str
        Directory to write the JSON result file.
    max_symbols : int
        Maximum symbols to test (default 5).
    
    Returns
    -------
    dict[str, Any]
        Summary of per-state metrics and per-symbol detail.
    """
    # Load config
    with open(config_path) as f:
        raw_cfg = yaml.safe_load(f)
    
    bt_cfg = raw_cfg.get("backtest", {})
    start = bt_cfg.get("start_date", "2024-01-01")
    end = bt_cfg.get("end_date", "2024-06-01")
    timeframe = bt_cfg.get("timeframe", "5min")
    universe = bt_cfg.get("universe", 
                          raw_cfg.get("collector", {}).get("universe", "NIFTY50"))
    
    # Build inference context (loads model, TimescaleDB, etc.)
    ctx = await build_inference_context()
    
    # Create PredictionModifier WITHOUT MVE — we'll control MVS injection manually
    # per D-18: single prediction pass, then compare by applying modifier
    modifier = PredictionModifier(mve=None)  # No live MVE — we'll inject mvs_dict
    
    # Use a simple MVS dict for modification. This represents a moderate
    # bearish market state (~-0.3 composite, VIX=20)
    default_mvs = {
        "composite": -0.30,
        "market_state": "fear",
        "vix_value": 20.0,
        "temperature_adjustment": 0.075,  # (20-15)*0.015
        "directional_bias": -0.30,
        "band_width_multiplier": 1.04,    # 1.0 + (20-15)*0.008
        "signal_threshold": 0.006,        # 0.005 + (20-15)*0.0002
        "confidence_override": "LOW",
        "created_at": datetime.utcnow().isoformat(),
        "dimensions": [],
    }
    
    symbols = list(
        get_universe(universe).keys()
    )[:max_symbols]
    
    results: dict[str, Any] = {
        "config": {"start": start, "end": end, "timeframe": timeframe, "universe": universe},
        "symbols": {},
        "summary": {},
    }
    
    all_unmodified = {"mae": [], "dacc": [], "confidence": []}
    all_modified = {"mae": [], "dacc": [], "confidence": []}
    
    for symbol in symbols:
        logger.info("Processing %s ...", symbol)
        
        # Fetch candles
        df = await ctx.db.get_candles(
            symbol,
            timeframe,
            limit=5000,
            start_date=datetime.fromisoformat(start),
            end_date=datetime.fromisoformat(end),
        )
        if df.empty or len(df) < 50:
            logger.warning("Skipping %s — insufficient data", symbol)
            continue
        
        # Build context and predict
        try:
            context = await ctx.context_builder.build(symbol, timeframe, "BACKTEST")
            result = await ctx.engine.predict(
                symbol=symbol,
                df=context["df"],
                x_ts=context["x_ts"],
                y_ts=context["y_ts"],
                timeframe=timeframe,
                mode="BACKTEST",
                skip_dqg=True,
            )
        except Exception:
            logger.exception("Predict failed for %s", symbol)
            continue
        
        # Actual close values (for computing accuracy)
        pred_close = result.get("pred_close", [])
        actual_close = df["close"].astype(float).tolist()[-len(pred_close):]
        
        if not pred_close or not actual_close:
            continue
        
        # ── State 1: Unmodified (raw prediction) ───────────────────────────
        unmodified_result = dict(result)  # shallow copy
        
        # ── State 2: Modified with MVE ─────────────────────────────────────
        modified_result = dict(result)  # shallow copy
        # Inject a mock MVS into the modifier to simulate MVE being active
        mock_mve = _MockMVE(default_mvs)
        modifier_with_mve = PredictionModifier(mve=mock_mve)
        modified_result = modifier_with_mve.modify_post_inference(modified_result)
        
        # Compute metrics for both states
        unmodified_mae = compute_mae(actual_close, unmodified_result.get("pred_close", []))
        modified_mae = compute_mae(actual_close, modified_result.get("pred_close", []))
        
        unmodified_dacc = compute_directional_accuracy(actual_close, unmodified_result.get("pred_close", []))
        modified_dacc = compute_directional_accuracy(actual_close, modified_result.get("pred_close", []))
        
        unmodified_conf = _parse_confidence(unmodified_result.get("confidence", "MEDIUM"))
        modified_conf = _parse_confidence(modified_result.get("mve_confidence", modified_result.get("confidence", "MEDIUM")))
        
        symbol_result = {
            "unmodified": {
                "mae": unmodified_mae,
                "directional_accuracy": unmodified_dacc,
                "confidence_avg": unmodified_conf,
            },
            "modified": {
                "mae": modified_mae,
                "directional_accuracy": modified_dacc,
                "confidence_avg": modified_conf,
            },
            "difference": {
                "mae": (modified_mae - unmodified_mae) if modified_mae is not None and unmodified_mae is not None else None,
                "directional_accuracy": (modified_dacc - unmodified_dacc) if modified_dacc is not None and unmodified_dacc is not None else None,
            },
            "total_bars": len(pred_close),
        }
        
        results["symbols"][symbol] = symbol_result
        
        # Accumulate for global summary
        if unmodified_mae is not None:
            all_unmodified["mae"].append(unmodified_mae)
            all_modified["mae"].append(modified_mae)
        if unmodified_dacc is not None:
            all_unmodified["dacc"].append(unmodified_dacc)
            all_modified["dacc"].append(modified_dacc)
        all_unmodified["confidence"].append(unmodified_conf)
        all_modified["confidence"].append(modified_conf)
    
    # ── Summary ────────────────────────────────────────────────────────────
    def _avg(vals: list) -> float | None:
        return sum(vals) / len(vals) if vals else None
    
    summary = {
        "unmodified": {
            "mean_mae": _avg(all_unmodified["mae"]),
            "mean_directional_accuracy": _avg(all_unmodified["dacc"]),
            "mean_confidence": _avg(all_unmodified["confidence"]),
        },
        "modified": {
            "mean_mae": _avg(all_modified["mae"]),
            "mean_directional_accuracy": _avg(all_modified["dacc"]),
            "mean_confidence": _avg(all_modified["confidence"]),
        },
        "difference": {
            "mae": (_avg(all_modified["mae"]) - _avg(all_unmodified["mae"]))
                   if all_unmodified["mae"] and all_modified["mae"] else None,
            "directional_accuracy": (_avg(all_modified["dacc"]) - _avg(all_unmodified["dacc"]))
                                     if all_unmodified["dacc"] and all_modified["dacc"] else None,
        },
        "symbols_tested": len(results["symbols"]),
        "total_bars": sum(s["total_bars"] for s in results["symbols"].values()),
    }
    results["summary"] = summary
    
    # Write output
    os.makedirs(output_dir, exist_ok=True)
    output_path = os.path.join(output_dir, "mve_backtest_results.json")
    with open(output_path, "w") as f:
        json.dump(results, f, indent=2, default=str)
    
    # ── Console output ─────────────────────────────────────────────────────
    _print_summary(summary)
    
    # Cleanup
    await close_inference_context(ctx)
    
    return results


class _MockMVE:
    """Minimal mock MVE for backtest comparison (D-18).
    
    Provides the same interface as MarketVarianceEngine but with
    a fixed MVS dict for deterministic comparison.
    """
    
    def __init__(self, mvs_dict: dict[str, Any]) -> None:
        self._mvs_dict = mvs_dict
        self.is_ready = True
    
    @property
    def last_mvs(self) -> dict[str, Any]:
        return self._mvs_dict


def _parse_confidence(conf: str) -> float:
    """Map confidence string to numeric value for averaging."""
    mapping = {"HIGH": 1.0, "MEDIUM": 0.5, "LOW": 0.0, "PANIC": -0.5}
    return mapping.get(str(conf).upper(), 0.5)


def _print_summary(summary: dict[str, Any]) -> None:
    """Print a console table of results per D-22."""
    u = summary["unmodified"]
    m = summary["modified"]
    d = summary["difference"]
    
    print()
    print("=" * 70)
    print("  MVE BACKTEST RESULTS")
    print("=" * 70)
    print(f"  Symbols tested: {summary['symbols_tested']}")
    print(f"  Total bars:     {summary['total_bars']}")
    print()
    print(f"  {'Metric':<30} {'Unmodified':<14} {'Modified':<14} {'Diff':<10}")
    print(f"  {'-'*30} {'-'*14} {'-'*14} {'-'*10}")
    
    def _fmt(val) -> str:
        if val is None:
            return "N/A"
        return f"{val:.6f}"
    
    print(f"  {'MAE (pred_close)':<30} {_fmt(u['mean_mae']):<14} {_fmt(m['mean_mae']):<14} {_fmt(d['mae']):<10}")
    print(f"  {'Directional Accuracy':<30} {_fmt(u['mean_directional_accuracy']):<14} {_fmt(m['mean_directional_accuracy']):<14} {_fmt(d['directional_accuracy']):<10}")
    print(f"  {'Avg Confidence':<30} {_fmt(u['mean_confidence']):<14} {_fmt(m['mean_confidence']):<14}")
    print()
    
    if d.get("mae") is not None:
        if d["mae"] < 0:
            print(f"  ✓ MVE improved MAE by {abs(d['mae']):.6f} (lower is better)")
        elif d["mae"] > 0:
            print(f"  ⚠ MVE increased MAE by {d['mae']:.6f}")
        else:
            print("  ∼ MVE had no effect on MAE")
    
    print("=" * 70)
    print(f"  Full results: backtest/output/mve_backtest_results.json")
    print()


def get_universe(name: str) -> dict:
    """Wrapper around scripts.seed_instruments.get_universe."""
    from scripts.seed_instruments import get_universe as _get_universe
    return _get_universe(name)


def main() -> None:
    parser = argparse.ArgumentParser(
        description="MVE Backtest: compare prediction accuracy with/without MVE"
    )
    parser.add_argument(
        "--config",
        default="config/base.yaml",
        help="Path to base YAML config (default: config/base.yaml)",
    )
    parser.add_argument(
        "--output-dir",
        default="backtest/output",
        help="Output directory for JSON results (default: backtest/output)",
    )
    parser.add_argument(
        "--max-symbols",
        type=int,
        default=5,
        help="Maximum symbols to test (default: 5)",
    )
    parser.add_argument("--verbose", action="store_true", help="Enable debug logging")
    args = parser.parse_args()
    
    logging.basicConfig(
        level=logging.DEBUG if args.verbose else logging.INFO,
        format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    )
    
    asyncio.run(run_backtest(args.config, args.output_dir, args.max_symbols))


if __name__ == "__main__":
    main()
```

**Important notes:**
- The script reuses `build_inference_context()` from `model.factory` (same as the FastAPI lifespan) for consistent model loading
- Uses `_MockMVE` class (defined in the script) to inject a fixed MVS dict — avoids needing a running MVE engine per D-18
- Default MVS represents a moderate bearish state (composite=-0.3, VIX=20) for a meaningful comparison
- All three states per D-20: Unmodified, Modified with MVE, Difference
- Output per D-22: console table + JSON file in backtest output directory
- Per D-19: metrics are MAE (primary), directional accuracy, average confidence
- Per D-21: symbols and timeframes from existing backtest config
</action>
  <verify>
    <automated>test -f /mnt/f/wsl/work/kronos-nse/scripts/backtest_mve.py && grep -q "def main" /mnt/f/wsl/work/kronos-nse/scripts/backtest_mve.py && echo "SCRIPT EXISTS"</automated>
  </verify>
  <done>scripts/backtest_mve.py created with CLI entrypoint, async runner, MockMVE helper, console table output, and JSON file output</done>
</task>

<task type="auto">
  <name>Task 2: Verify script imports and structure</name>
  <files>scripts/backtest_mve.py</files>
  <action>
Run a quick validation to ensure the script parses correctly and key components import cleanly.

The script itself is already created in Task 1. This task validates:
1. Python syntax is valid
2. Key classes/functions are defined
3. The _MockMVE helper works correctly

Run these checks:
```bash
# 1. Syntax check
python -c "import ast; ast.parse(open('scripts/backtest_mve.py').read()); print('SYNTAX OK')"

# 2. _MockMVE class works
python -c "
import sys; sys.path.insert(0, '.')
exec(open('scripts/backtest_mve.py').read().split('if __name__')[0])
m = _MockMVE({'composite': -0.3})
assert m.is_ready == True
assert m.last_mvs['composite'] == -0.3
print('MOCK MVE OK')
"

# 3. compute_mae and compute_directional_accuracy work
python -c "
import sys; sys.path.insert(0, '.')
exec(open('scripts/backtest_mve.py').read().split('if __name__')[0])
assert compute_mae([1.0, 2.0], [1.1, 1.9]) is not None
assert compute_mae([], []) is None
d = compute_directional_accuracy([1.0, 2.0, 1.5], [1.0, 2.1, 1.4])
assert d is not None and 0 <= d <= 1.0
print('METRICS OK')
"
```

**Note:** If any import errors occur that require actual database connections (like `build_inference_context`), those are expected — the script is designed to run with a live config. The structure validation above only checks syntax and standalone logic.
</action>
  <verify>
    <automated>/mnt/f/wsl/work/kronos-nse/.venv-linux/bin/python -c "import ast; ast.parse(open('scripts/backtest_mve.py').read()); print('SYNTAX OK')"</automated>
  </verify>
  <done>Script is syntactically valid, _MockMVE class works, compute_mae/directional_accuracy functions return correct values</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → script | User-provided config path and output directory |
| Script → TimescaleDB | Read-only queries via inference context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering | --config path | accept | CLI tool — user controls execution environment. Path traversal limited to local file reads. |
| T-09-12 | Repudiation | JSON output | accept | No audit trail needed for backtest results. Output is informational only. |
</threat_model>

<verification>
```bash
# 1. Script syntax is valid
/mnt/f/wsl/work/kronos-nse/.venv-linux/bin/python -c "import ast; ast.parse(open('scripts/backtest_mve.py').read()); print('SYNTAX OK')"

# 2. MockMVE class works standalone
/mnt/f/wsl/work/kronos-nse/.venv-linux/bin/python -c "
import sys; sys.path.insert(0, '.')
exec(open('scripts/backtest_mve.py').read().split('if __name__')[0])
m = _MockMVE({'composite': -0.3, 'directional_bias': -0.3})
assert m.is_ready == True
assert m.last_mvs['composite'] == -0.3
assert m.last_mvs['directional_bias'] == -0.3
print('MOCK OK')
"

# 3. Metric functions work
/mnt/f/wsl/work/kronos-nse/.venv-linux/bin/python -c "
import sys; sys.path.insert(0, '.')
exec(open('scripts/backtest_mve.py').read().split('if __name__')[0])
mae = compute_mae([1.0, 2.0, 3.0], [0.9, 2.1, 2.8])
assert mae is not None and mae > 0
assert compute_mae([], []) is None
da = compute_directional_accuracy([1.0, 2.0, 1.5, 1.8], [1.0, 1.9, 1.6, 1.7])
assert da is not None and 0 <= da <= 1.0
print('METRICS OK')
"
```
</verification>

<success_criteria>
- [ ] `scripts/backtest_mve.py` created with CLI argparse (--config, --output-dir, --max-symbols, --verbose)
- [ ] `_MockMVE` helper provides `is_ready=True` and `last_mvs` property with fixed MVS dict
- [ ] `compute_mae()` and `compute_directional_accuracy()` metrics functions
- [ ] Script compares 3 states: Unmodified, Modified, Difference per D-20
- [ ] Console summary table printed with MAE, directional accuracy, confidence per D-22
- [ ] JSON output written to backtest/output/mve_backtest_results.json
- [ ] All decisions D-18 through D-22 implemented
</success_criteria>

<output>
After completion, create `.planning/phases/09-dqg-system-test/09-04-SUMMARY.md`
</output>
